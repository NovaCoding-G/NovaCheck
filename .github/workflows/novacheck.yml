name: NovaCheck

on:
  pull_request:
  push:
    branches: [main]
  # Registry state changes even when the code does not: a name that was
  # unregistered last week can be owned by someone else today.
  schedule:
    - cron: "17 6 * * 1"

permissions:
  contents: read
  security-events: write

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0
      - uses: ./
        with:
          version: local
          changed: ${{ github.event_name == 'pull_request' }}
          upload-sarif: true
          fail-below: "85"
          fail-on-incomplete: true
